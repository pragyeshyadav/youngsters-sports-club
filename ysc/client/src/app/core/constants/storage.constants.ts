/** localStorage key for persisted auth session (JWT-ready shape). */
export const AUTH_SESSION_STORAGE_KEY = 'ysc_auth_session';

/** GIS credential (JWT) — used by AuthGuard and quick restore. */
export const GOOGLE_TOKEN_STORAGE_KEY = 'google_token';
